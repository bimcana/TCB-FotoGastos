# Fase 2E Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Autorecorte de importación con IA local (U²-Net-p ONNX), revisor Gemini visible con reintento manual, reconexión automática de Drive y panel de cola de subida; más el fix del falso positivo del rescate en cámara en vivo.

**Architecture:** `src/detectia.js` (nuevo) corre U²-Net-p con onnxruntime-web WASM (vendorizado, carga perezosa, SW runtime-cache) y reutiliza el pipeline OpenCV de `detect.js` para convertir la máscara en esquinas. El revisor de Fase 2C/2D gana estado observable (`archivosEnLectura`) y disparo post-subida; `actualizarEntradaConReArchivo` devuelve además la entrada final para rellenar el panel abierto. Reconexión con `requestAccessToken({prompt:''})`.

**Tech Stack:** onnxruntime-web 1.27.0 (WASM SIMD), U²-Net-p ONNX (~4.7 MB, Apache-2.0), OpenCV.js, Gemini API, Drive API v3, node --test.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-15-fase2e-design.md`.
- Commits en español con `git commit -F`; repo sin datos de BIMCANA.
- `sw.js` → `VERSION = 'fase2e-v1'` al final (Task 9).
- `ort.env.wasm.numThreads = 1` (GitHub Pages no envía COOP/COEP).
- Identificadores ASCII; comentarios en español.
- Rama `fase2e` desde `main`; merge --no-ff al final.

---

### Task 1: Vendorizar onnxruntime-web y el modelo U²-Net-p

**Files:**
- Create: `vendor/ort/ort.wasm.min.js`, `vendor/ort/ort-wasm-simd-threaded.wasm`, `vendor/ort/ort-wasm-simd-threaded.mjs`, `vendor/modelos/u2netp.onnx`
- Modify: `.gitattributes`

**Interfaces:**
- Produces: assets en `vendor/ort/` y `vendor/modelos/` que Task 3 carga por URL relativa.

- [ ] **Step 1: Descargar los 4 archivos**

```bash
cd TCB-FotoGastos
mkdir -p vendor/ort vendor/modelos
curl -L -o vendor/ort/ort.wasm.min.js "https://cdn.jsdelivr.net/npm/onnxruntime-web@1.27.0/dist/ort.wasm.min.js"
curl -L -o vendor/ort/ort-wasm-simd-threaded.wasm "https://cdn.jsdelivr.net/npm/onnxruntime-web@1.27.0/dist/ort-wasm-simd-threaded.wasm"
curl -L -o vendor/ort/ort-wasm-simd-threaded.mjs "https://cdn.jsdelivr.net/npm/onnxruntime-web@1.27.0/dist/ort-wasm-simd-threaded.mjs"
curl -L -o vendor/modelos/u2netp.onnx "https://github.com/danielgatis/rembg/releases/download/v0.0.0/u2netp.onnx"
```

- [ ] **Step 2: Verificar tamaños** — `ls -l` esperado: ort.wasm.min.js ~50 KB, wasm ~13.5 MB, mjs ~24 KB, u2netp.onnx ~4.7 MB. Si u2netp.onnx < 1 MB es una página de error: abortar y revisar la URL.
- [ ] **Step 3: `.gitattributes`** — añadir:

```
vendor/ort/** binary
vendor/modelos/** binary
```

- [ ] **Step 4: Commit** — `Fase 2E - Task 1: vendorizar onnxruntime-web 1.27 y modelo U2-Net-p`

---

### Task 2: detect.js — máscara→esquinas, rescate opcional y guardas del vivo

**Files:**
- Modify: `src/detect.js`, `src/main.js` (bucle en vivo), `tests/detect.test.js`

**Interfaces:**
- Consumes: `aCuatroEsquinas(c, area)` interna existente (se le añade parámetro `rescate`).
- Produces:
  - `mapearEsquinas(pts, sx, sy) → pts` (puro)
  - `tocaBorde(esquinas, w, h, margen = 0.01) → boolean` (puro)
  - `esquinasDeMascara(maskCanvas, wOrig, hOrig) → esquinas|null` (cv)
  - `detectarDocumento(srcCanvas, maxLado = 700, opciones = {}) → esquinas|null` con `opciones.rescate` (default `true`)

- [ ] **Step 1: Tests puros que fallan** — añadir a `tests/detect.test.js`:

```js
import { mapearEsquinas, tocaBorde } from '../src/detect.js';

test('mapearEsquinas escala x/y de forma independiente', () => {
  assert.deepEqual(mapearEsquinas([{x:10,y:20}], 2, 0.5), [{x:20,y:10}]);
});

test('tocaBorde detecta esquinas pegadas al borde del frame', () => {
  const dentro = [{x:50,y:50},{x:950,y:50},{x:950,y:950},{x:50,y:950}];
  assert.equal(tocaBorde(dentro, 1000, 1000), false);
  assert.equal(tocaBorde([{x:5,y:500},...dentro.slice(1)], 1000, 1000), true);  // x en el 1%
  assert.equal(tocaBorde([{x:50,y:996},...dentro.slice(1)], 1000, 1000), true); // y al fondo
});
```

- [ ] **Step 2: `npm test` → FAIL (no exportadas).**
- [ ] **Step 3: Implementar en `src/detect.js`:**

(a) Helpers puros junto a `escalaTrabajo`:

```js
export function mapearEsquinas(pts, sx, sy){
  return pts.map(p => ({ x: p.x * sx, y: p.y * sy }));
}

// true si alguna esquina queda a menos de `margen` (fraccion) del borde del frame.
// La camara en vivo lo usa para descartar falsos positivos (fondos texturados que
// producen cuadrilateros pegados a los bordes del encuadre).
export function tocaBorde(esquinas, w, h, margen = 0.01){
  const mx = w * margen, my = h * margen;
  return esquinas.some(p => p.x <= mx || p.y <= my || p.x >= w - mx || p.y >= h - my);
}
```

(b) `aCuatroEsquinas(c, area, rescate = true)` — la fase de casco convexo solo corre `if (rescate)` (envolver el bloque `let hull; try { ... } finally` en `if (!rescate) return null;` antes).

(c) `cuadrilateroDeBinaria(th, escala, minArea, rescate)` pasa el flag; `detectarDocumento(srcCanvas, maxLado = 700, opciones = {})` hace `const { rescate = true } = opciones;` y llama `cuadrilateroDeBinaria(th, escala, w * h * 0.12, rescate)`.

(d) `esquinasDeMascara` (nueva, exportada):

```js
// Mascara binaria (canvas) → 4 esquinas en coords de la imagen original. La usa el
// motor IA: la mascara de U2-Net-p es limpia, asi que el contorno mayor + rescate
// bastan; se valida con la misma guarda que la deteccion clasica.
export function esquinasDeMascara(maskCanvas, wOrig, hOrig){
  let mat, gray, kernel, contours, hier;
  try {
    mat = cv.imread(maskCanvas);
    gray = new cv.Mat();
    cv.cvtColor(mat, gray, cv.COLOR_RGBA2GRAY);
    kernel = cv.getStructuringElement(cv.MORPH_RECT, new cv.Size(9, 9));
    cv.morphologyEx(gray, gray, cv.MORPH_CLOSE, kernel);
    cv.threshold(gray, gray, 127, 255, cv.THRESH_BINARY);
    contours = new cv.MatVector();
    hier = new cv.Mat();
    cv.findContours(gray, contours, hier, cv.RETR_EXTERNAL, cv.CHAIN_APPROX_SIMPLE);
    let mejor = null, mejorArea = maskCanvas.width * maskCanvas.height * 0.05;
    for (let i = 0; i < contours.size(); i++){
      let c;
      try {
        c = contours.get(i);
        const area = cv.contourArea(c);
        if (area > mejorArea){
          const pts = aCuatroEsquinas(c, area);
          if (pts){ mejorArea = area; mejor = pts; }
        }
      } finally { if (c) c.delete(); }
    }
    if (!mejor) return null;
    const ordenado = ordenarEsquinas(
      mapearEsquinas(mejor, wOrig / maskCanvas.width, hOrig / maskCanvas.height));
    return cuadrilateroValido(ordenado, wOrig, hOrig) ? ordenado : null;
  } finally {
    if (kernel) kernel.delete();
    if (hier) hier.delete();
    if (contours) contours.delete();
    if (gray) gray.delete();
    if (mat) mat.delete();
  }
}
```

- [ ] **Step 4: Cámara en vivo estricta** — en `src/main.js`, `buclDeteccion`:

```js
      let esquinas = detectarDocumento(frame, 700, { rescate: false });
      if (esquinas && tocaBorde(esquinas, frame.width, frame.height)) esquinas = null;
```

(cambia `const esquinas` por `let`; importar `tocaBorde` en la línea de imports de detect.js).

- [ ] **Step 5: `npm test` → PASS.**
- [ ] **Step 6: Commit** — `Fase 2E - Task 2: esquinasDeMascara, rescate opcional y camara en vivo estricta`

---

### Task 3: `src/detectia.js` — U²-Net-p con onnxruntime-web

**Files:**
- Create: `src/detectia.js`

**Interfaces:**
- Consumes: `esquinasDeMascara` (Task 2); assets de Task 1.
- Produces: `detectarConIA(canvas) → Promise<esquinas|null>` (NUNCA lanza; null si falla).

- [ ] **Step 1: Crear `src/detectia.js`:**

```js
// Motor IA de deteccion (Fase 2E): U2-Net-p sobre ONNX Runtime Web (WASM). Carga
// perezosa: ni el runtime (~13 MB) ni el modelo (~4.7 MB) se descargan hasta que hacen
// falta; el service worker los cachea para uso offline posterior. Si algo falla
// (sin red la primera vez, iOS sin WASM SIMD), devuelve null y el flujo sigue sin IA.
import { esquinasDeMascara } from './detect.js';

const LADO = 320;
let sesionProm = null;

function cargarScript(src){
  return new Promise((res, rej) => {
    const s = document.createElement('script');
    s.src = src; s.onload = res; s.onerror = () => rej(new Error('No se pudo cargar ' + src));
    document.head.appendChild(s);
  });
}

function sesion(){
  if (!sesionProm){
    sesionProm = (async () => {
      if (!window.ort) await cargarScript('vendor/ort/ort.wasm.min.js');
      ort.env.wasm.wasmPaths = 'vendor/ort/';
      ort.env.wasm.numThreads = 1; // GitHub Pages no envia COOP/COEP: sin hilos
      return ort.InferenceSession.create('vendor/modelos/u2netp.onnx',
        { executionProviders: ['wasm'] });
    })().catch(e => { sesionProm = null; throw e; }); // reintentable en la proxima llamada
  }
  return sesionProm;
}

// canvas → Float32Array NCHW 1x3x320x320 (preprocesado estandar U2-Net: /255 y
// normalizacion mean/std de ImageNet, mismo pipeline que usa rembg).
function aTensor(canvas){
  const c = document.createElement('canvas');
  c.width = LADO; c.height = LADO;
  const ctx = c.getContext('2d');
  ctx.drawImage(canvas, 0, 0, LADO, LADO);
  const { data } = ctx.getImageData(0, 0, LADO, LADO);
  const MEAN = [0.485, 0.456, 0.406], STD = [0.229, 0.224, 0.225];
  const t = new Float32Array(3 * LADO * LADO);
  for (let i = 0; i < LADO * LADO; i++){
    for (let ch = 0; ch < 3; ch++){
      t[ch * LADO * LADO + i] = (data[i * 4 + ch] / 255 - MEAN[ch]) / STD[ch];
    }
  }
  return t;
}

// Salida del modelo (320x320, valores ~0..1) → canvas de mascara binaria
// (normalizacion min-max + umbral 0.5, como el postprocesado de referencia).
function mascaraACanvas(salida){
  let min = Infinity, max = -Infinity;
  for (const v of salida){ if (v < min) min = v; if (v > max) max = v; }
  const rango = (max - min) || 1;
  const c = document.createElement('canvas');
  c.width = LADO; c.height = LADO;
  const ctx = c.getContext('2d');
  const img = ctx.createImageData(LADO, LADO);
  for (let i = 0; i < LADO * LADO; i++){
    const on = ((salida[i] - min) / rango) >= 0.5 ? 255 : 0;
    img.data[i * 4] = img.data[i * 4 + 1] = img.data[i * 4 + 2] = on;
    img.data[i * 4 + 3] = 255;
  }
  ctx.putImageData(img, 0, 0);
  return c;
}

export async function detectarConIA(canvas){
  try {
    const s = await sesion();
    const tensor = new ort.Tensor('float32', aTensor(canvas), [1, 3, LADO, LADO]);
    const out = await s.run({ [s.inputNames[0]]: tensor });
    const salida = out[s.outputNames[0]].data; // d0: 1x1x320x320
    return esquinasDeMascara(mascaraACanvas(salida), canvas.width, canvas.height);
  } catch(e){
    console.error('detectarConIA:', e);
    return null;
  }
}
```

- [ ] **Step 2: `node --check src/detectia.js` → OK. `npm test` → PASS (sin uso aún).**
- [ ] **Step 3: Commit** — `Fase 2E - Task 3: detectarConIA con U2-Net-p (ONNX WASM, carga perezosa)`

---

### Task 4: Flujos de captura/importación con IA y editor precargado

**Files:**
- Modify: `src/main.js`

**Interfaces:**
- Consumes: `detectarConIA` (T3), `abrirEditorEsquinas` (2D), `detectarDocumento(canvas, 1200)` (T2).

- [ ] **Step 1: Import** — junto al import de esquinas.js: `import { detectarConIA } from './detectia.js';`

- [ ] **Step 2: Importación estilo Adobe** — en `cargarSiguienteDelLote`, reemplazar el cuerpo del `try`:

```js
    const canvas = await archivoACanvas(lote.files[lote.i]);
    // Autorecorte: clasico (rapido) → IA local si fallo. El editor abre SIEMPRE con lo
    // detectado precargado; "Aplicar" acepta el recorte y se pasa a los datos (Adobe Scan).
    let esquinas = detectarDocumento(canvas, 1200);
    if (!esquinas) esquinas = await detectarConIA(canvas);
    esquinas = await abrirEditorEsquinas(canvas, esquinas);
    window.__captura = { canvas, esquinas };
    procesarYRevisar();
```

(Cancelar el editor → `esquinas = null` → Revisión muestra el original sin recorte, como hoy.)

- [ ] **Step 3: Disparo manual con IA de respaldo** — reemplazar el listener de `#shutter`:

```js
document.getElementById('shutter').addEventListener('click', async () => {
  if (disparando) return;
  if (!video.videoWidth) return toast('La cámara no está lista');
  const canvas = capturarFrame(video);
  const fx = document.getElementById('flashfx');
  fx.classList.remove('go'); void fx.offsetWidth; fx.classList.add('go');
  // Sin deteccion en vivo: reintenta sobre el still (con rescate) y luego con la IA local.
  let esquinas = ultimasEsquinas || detectarDocumento(canvas, 1200) || await detectarConIA(canvas);
  window.__captura = { canvas, esquinas };
  procesarYRevisar();
});
```

- [ ] **Step 4: `node --check src/main.js` → OK; `npm test` → PASS.**
- [ ] **Step 5: Commit** — `Fase 2E - Task 4: autorecorte con IA en importacion y disparo manual`

---

### Task 5: Revisor visible (chip animado, relleno del panel abierto, disparo post-subida)

**Files:**
- Modify: `src/main.js`, `styles.css`

**Interfaces:**
- Consumes: `actualizarEntradaConReArchivo` (2D) — **se modifica** para devolver también `entrada`.
- Produces: `archivosEnLectura: Set<string>`; `rellenarPanel(f)`; `refrescarGastosSiVisible()`; `alTerminarLectura(archivoAnterior, res)`.

- [ ] **Step 1: `actualizarEntradaConReArchivo` devuelve la entrada final** — en sus dos `return` con datos:
  - camino sin re-archivo: `return { nombreFinal: archivo, estado: f.estado, movidaA: null, entrada: f };`
  - camino con re-archivo: `return { nombreFinal, estado: entradaFinal.estado, movidaA: ..., entrada: entradaFinal };`

- [ ] **Step 2: Estado observable + helpers** — junto al mutex en main.js:

```js
// Archivos que el revisor esta leyendo AHORA (para el chip "Leyendo con IA…" en Gastos
// y para rellenar el panel abierto al terminar).
const archivosEnLectura = new Set();

function refrescarGastosSiVisible(){
  if (document.getElementById('scr-gastos').classList.contains('active')) refrescarGastos();
}

function alTerminarLectura(archivoAnterior, res){
  archivosEnLectura.delete(archivoAnterior);
  refrescarGastosSiVisible();
  if (!res) return;
  // Si el usuario tiene abierto el panel de ESTA factura (aunque se haya renombrado),
  // se rellena ante sus ojos; la confirmacion sigue siendo suya.
  if (rvArchivo && (rvArchivo === archivoAnterior || rvArchivo === res.nombreFinal)){
    rvArchivo = res.nombreFinal;
    rellenarPanel(res.entrada);
    toast('Datos leídos — revisa y confirma');
  }
}
```

- [ ] **Step 3: `rellenarPanel(f)`** — extraer de `abrirRevisar` (y usarla ahí):

```js
function rellenarPanel(f){
  document.getElementById('revisar-titulo').textContent = `Revisar ${f.archivo}`;
  for (const [id, campo] of Object.entries(RV_CAMPOS)){
    document.getElementById(id).value = f[campo] != null ? f[campo] : '';
  }
}
```

- [ ] **Step 4: Revisor marca y notifica** — en `revisarPendientes`, alrededor del procesamiento de cada `item`: tras obtener `canvas` OK: `archivosEnLectura.add(item.archivo); refrescarGastosSiVisible();`. En el bloque del helper:

```js
      try {
        const res = await actualizarEntradaConReArchivo(item.mesId, item.archivo, f => { ... igual ... });
        await eliminarRevision(item.id);
        alTerminarLectura(item.archivo, res);
      } catch(e){ archivosEnLectura.delete(item.archivo); console.error(e); break; }
```

Y en el `break` del error de Gemini (catch de `extraerDatos`) también `archivosEnLectura.delete(item.archivo);` antes del `break`.

- [ ] **Step 5: Chip animado en Gastos** — en `refrescarGastos`, antes del chip de estado:

```js
        if (archivosEnLectura.has(n)){
          const chip = document.createElement('span');
          chip.className = 'chip info leyendo';
          chip.style.cssText = 'margin-top:4px; font-size:10px; padding:2px 8px';
          chip.innerHTML = '<span class="dot"></span>Leyendo con IA…';
          amt.appendChild(chip);
        } else if (est){ ... chip de estado existente ... }
```

CSS al final de `styles.css`:

```css
/* --- Fase 2E: chip animado del revisor --- */
.chip.leyendo .dot{ animation: leyendo-pulso 1.1s ease-in-out infinite; }
@keyframes leyendo-pulso{ 0%,100%{opacity:.35; transform:scale(.8)} 50%{opacity:1; transform:scale(1.15)} }
```

- [ ] **Step 6: Disparo post-subida** — en el handler de `confirm-btn`, tras el toast de éxito: `revisarPendientes();` (sin await). Al final de `procesarCola` (tras `actualizarBadge()`): `revisarPendientes();`.

- [ ] **Step 7: `npm test` → PASS; commit** — `Fase 2E - Task 5: revisor visible con chip animado y relleno del panel`

---

### Task 6: Botón "Leer con IA" en el panel de revisión

**Files:**
- Modify: `index.html`, `src/main.js`

**Interfaces:**
- Consumes: `pendientesRevision`/`eliminarRevision` (revision.js), `thumbCache`, `archivoACanvas`, `extraerDatos`/`extraerDatosLocal`, `actualizarEntradaConReArchivo`, `rellenarPanel`.

- [ ] **Step 1: HTML** — la fila de acciones del `#revisar-panel` queda:

```html
      <div style="display:flex; gap:8px; margin-top:12px">
        <button class="btn btn-ghost btn-sm" id="rv-leer" style="flex:1">Leer con IA</button>
        <button class="btn btn-ghost btn-sm" id="rv-ver-imagen" style="flex:1">Ver imagen</button>
        <button class="btn btn-primary btn-sm" id="rv-confirmar" style="flex:1">Confirmar</button>
      </div>
```

- [ ] **Step 2: Visibilidad** — en `abrirRevisar`, tras `rellenarPanel(f)`: `document.getElementById('rv-leer').hidden = f.estado === 'completa';`

- [ ] **Step 3: Handler en main.js:**

```js
// Reintento manual (idea de Ari): lee ESTA factura al momento — Gemini si hay key y
// conexion, OCR local si no — y deja el resultado 'pendiente' para que el usuario valide.
async function leerConIAAhora(){
  const ctx = window.__gastosMes;
  if (!ctx || !rvArchivo) return;
  const archivo = rvArchivo;
  const btn = document.getElementById('rv-leer');
  btn.disabled = true; btn.textContent = 'Leyendo…';
  try {
    const item = (await pendientesRevision()).find(x => x.archivo === archivo);
    let blob = item ? item.blob : thumbCache.get(archivo);
    if (!blob){
      blob = await descargarImagen(ctx.mesId, archivo);
      if (blob) thumbCache.set(archivo, blob);
    }
    if (!blob) return toast('No se encontró la imagen de la factura');
    const canvas = await archivoACanvas(blob);
    const key = get('geminiKey', '');
    let datos = null, motor = null;
    if (key){
      try { datos = await extraerDatos(canvas, key, geminiModelo); motor = 'gemini'; }
      catch(e){ console.error(e); }
    }
    if (!datos){
      try { datos = await extraerDatosLocal(canvas); motor = 'local'; }
      catch(e){ console.error(e); }
    }
    if (!datos) return toast('Sin conexión y sin OCR disponible — intenta luego');
    const res = await actualizarEntradaConReArchivo(ctx.mesId, archivo, f => {
      for (const c of ['fechaEmision', 'ncf', 'rncEmisor', 'nombreComercio', 'subtotal', 'itbis', 'total']){
        if (datos[c] != null && datos[c] !== '') f[c] = datos[c];
      }
      if (motor === 'gemini') f.revisadaIA = true;
      f.estado = facturaCompleta(f) ? 'pendiente' : 'incompleta';
    });
    if (!res) return toast('La factura ya no está en el índice');
    if (item) await eliminarRevision(item.id); // ya leida: fuera de la cola de revision
    rvArchivo = res.nombreFinal;
    rellenarPanel(res.entrada);
    refrescarGastos();
    toast('Datos leídos — revisa y confirma');
  } catch(e){ console.error(e); toast('No se pudo leer: ' + e.message); }
  finally { btn.disabled = false; btn.textContent = 'Leer con IA'; }
}
document.getElementById('rv-leer').addEventListener('click', leerConIAAhora);
```

- [ ] **Step 4: `npm test` → PASS; commit** — `Fase 2E - Task 6: boton Leer con IA en el panel de revision`

---

### Task 7: Reconexión automática de Drive + aviso tocable + 401

**Files:**
- Modify: `src/drive.js`, `src/main.js`, `styles.css`

**Interfaces:**
- Produces (drive.js): `conectar(opciones = {silencioso?:boolean})`; `alDesconectar(cb)`; `api()` limpia el token y avisa en 401.
- Produces (main.js): `postConexion()` compartida; `reconectarSilencioso()`; `gastos-sub` tocable.

- [ ] **Step 1: drive.js** —

```js
let onDesconexion = null;
export function alDesconectar(cb){ onDesconexion = cb; }
```

En `api()`, antes del `if (!r.ok)`:

```js
  if (r.status === 401){ // token vencido a mitad de sesion: avisar para reconectar
    accessToken = null;
    if (onDesconexion) onDesconexion();
  }
```

`conectar` con opciones y timeout corto en silencioso:

```js
export function conectar(opciones = {}){
  return new Promise((res, rej) => {
    if (!tokenClient) return rej(new Error('Falta el Client ID en Ajustes'));
    const silencioso = opciones.silencioso || conectado();
    const timer = setTimeout(() => rej(new Error('Tiempo de espera agotado al conectar con Google')),
      opciones.silencioso ? 8000 : 60000);
    tokenClient.callback = t => { ... igual ... };
    tokenClient.requestAccessToken({ prompt: silencioso ? '' : 'consent' });
  });
}
```

- [ ] **Step 2: main.js — postConexion compartida** (reemplaza el cuerpo del handler de `btn-conectar`):

```js
async function postConexion(){
  const raizId = await asegurarCarpeta(get('carpetaRaiz', 'Gastos_NCF'));
  set('carpetaRaizId', raizId);
  set('driveConectadoAntes', true);
  document.getElementById('drive-estado').textContent =
    `Conectado ✓ — carpeta «${get('carpetaRaiz', 'Gastos_NCF')}» lista`;
  const sub = document.getElementById('gastos-sub');
  sub.textContent = 'Google Drive · conectado';
  sub.classList.remove('accion');
  refrescarGastos();
  procesarCola();
  revisarPendientes();
}

document.getElementById('btn-conectar').addEventListener('click', async () => {
  const btn = document.getElementById('btn-conectar');
  const clientId = get('clientId', '');
  if (!clientId) return toast('Pega primero tu Client ID de Google');
  btn.disabled = true;
  try {
    initAuth(clientId);
    await conectar();
    await postConexion();
    toast('Google Drive conectado');
  } catch(e){
    console.error(e);
    toast('No se pudo conectar: ' + e.message);
  } finally { btn.disabled = false; }
});
```

- [ ] **Step 3: Aviso tocable + reconexión silenciosa:**

```js
function mostrarAvisoReconectar(){
  const sub = document.getElementById('gastos-sub');
  sub.textContent = 'Reconectar Google Drive ▸';
  sub.classList.add('accion');
}
alDesconectar(mostrarAvisoReconectar);

document.getElementById('gastos-sub').addEventListener('click', async () => {
  if (conectado()) return;
  const clientId = get('clientId', '');
  if (!clientId) return toast('Pega tu Client ID de Google en Ajustes');
  try { initAuth(clientId); await conectar(); await postConexion(); toast('Google Drive conectado'); }
  catch(e){ console.error(e); toast('No se pudo conectar: ' + e.message); }
});

// Al abrir la app: si ya hubo consentimiento antes, renovar el acceso sin molestar.
// Google lo permite con prompt:'' mientras la sesion siga viva; si exige interaccion
// (o el popup se bloquea), queda el aviso tocable en Gastos.
async function reconectarSilencioso(){
  const clientId = get('clientId', '');
  if (!clientId || !get('driveConectadoAntes', false) || conectado()) return;
  if (!window.google){ mostrarAvisoReconectar(); return; } // GIS aun no cargo
  try {
    initAuth(clientId);
    await conectar({ silencioso: true });
    await postConexion();
    toast('Google Drive reconectado ✓');
  } catch(e){
    console.warn('Reconexion silenciosa fallo:', e.message);
    mostrarAvisoReconectar();
  }
}
window.addEventListener('load', () => setTimeout(reconectarSilencioso, 600));
```

- [ ] **Step 4: CSS** — `styles.css`: `.hsub.accion{ color:#4E9BEB; cursor:pointer; font-weight:650; }`

- [ ] **Step 5: `npm test` → PASS; commit** — `Fase 2E - Task 7: reconexion automatica de Drive y aviso tocable`

---

### Task 8: Panel de cola de subida

**Files:**
- Modify: `index.html`, `src/main.js`, `styles.css`

**Interfaces:**
- Consumes: `pendientes()`, `eliminar(id)` (queue.js), `procesarCola`, `actualizarBadge`, `normalizarFecha`.

- [ ] **Step 1: HTML** — antes de `#revisar-panel`:

```html
  <div class="revisar-panel" id="cola-panel" hidden>
    <div class="revisar-card">
      <div class="revisar-head">
        <b>Cola de subida</b>
        <button class="revisar-x" id="cola-cerrar" aria-label="Cerrar">✕</button>
      </div>
      <div id="cola-lista"></div>
      <button class="btn btn-primary btn-sm" id="cola-subir" style="width:100%; margin-top:12px">Subir ahora</button>
    </div>
  </div>
```

- [ ] **Step 2: CSS:**

```css
/* --- Fase 2E: panel de cola de subida --- */
.cola-item{ display:flex; gap:10px; align-items:center; padding:9px 0; border-bottom:1px solid var(--line-soft); }
.cola-item img{ width:44px; height:56px; object-fit:cover; border-radius:6px; background:#000; }
.cola-info{ flex:1; display:grid; gap:2px; min-width:0; }
.cola-info b{ font-size:12.5px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
.cola-info span{ font-size:11px; color:var(--dim); }
.cola-borrar{ background:none; border:none; font-size:17px; cursor:pointer; padding:6px; }
```

- [ ] **Step 3: main.js:**

```js
// Panel de cola de subida: ver, reintentar y eliminar lo que espera conexion con Drive.
let colaURLs = [];
async function abrirCola(){
  const lista = document.getElementById('cola-lista');
  colaURLs.forEach(u => URL.revokeObjectURL(u)); colaURLs = [];
  lista.innerHTML = '';
  const items = await pendientes();
  document.getElementById('cola-subir').disabled = !items.length;
  if (!items.length){
    lista.innerHTML = '<div class="gem-note">Nada en cola — todo está en Drive.</div>';
  }
  for (const it of items){
    const fila = document.createElement('div');
    fila.className = 'cola-item';
    const img = document.createElement('img');
    const u = URL.createObjectURL(it.blob); colaURLs.push(u);
    img.src = u; img.alt = 'Miniatura';
    const d = it.datos || {};
    const partes = [d.nombreComercio, normalizarFecha(d.fechaEmision),
      d.total != null ? 'RD$ ' + Number(d.total).toLocaleString('es-DO', { minimumFractionDigits: 2 }) : null];
    const info = document.createElement('div');
    info.className = 'cola-info';
    info.innerHTML = `<b class="num"></b><span>Esperando conexión con Drive</span>`;
    info.querySelector('b').textContent = partes.filter(Boolean).join(' · ') || 'Sin datos leídos';
    const del = document.createElement('button');
    del.className = 'cola-borrar'; del.textContent = '🗑';
    del.setAttribute('aria-label', 'Eliminar de la cola');
    del.addEventListener('click', async () => {
      if (!confirm('¿Eliminar esta factura de la cola? La foto se descartará (aún no está en Drive).')) return;
      await eliminar(it.id);
      actualizarBadge();
      abrirCola(); // re-render
    });
    fila.appendChild(img); fila.appendChild(info); fila.appendChild(del);
    lista.appendChild(fila);
  }
  document.getElementById('cola-panel').hidden = false;
}
function cerrarCola(){
  document.getElementById('cola-panel').hidden = true;
  colaURLs.forEach(u => URL.revokeObjectURL(u)); colaURLs = [];
}
document.getElementById('btn-cola').addEventListener('click', abrirCola);
document.getElementById('cola-cerrar').addEventListener('click', cerrarCola);
document.getElementById('cola-subir').addEventListener('click', async () => {
  if (!conectado()) return toast('Sin conexión con Drive — usa "Reconectar" en Gastos');
  cerrarCola();
  await procesarCola();
  refrescarGastosSiVisible();
  toast('Cola procesada');
});
```

- [ ] **Step 4: `npm test` → PASS; commit** — `Fase 2E - Task 8: panel de cola de subida (ver, reintentar, eliminar)`

---

### Task 9: SW, verificación integral, deploy y memoria

**Files:**
- Modify: `sw.js`, `README.md`

- [ ] **Step 1: sw.js** — `VERSION = 'fase2e-v1'`; `'src/detectia.js'` al PRECACHE; el branch de runtime-cache de tesseract se generaliza:

```js
  if (/\/vendor\/(tesseract|ort|modelos)\//.test(e.request.url)){
```

(comentario: binarios grandes de Tesseract/ONNX se cachean al usarse, no en precache).

- [ ] **Step 2: `npm test` → PASS.**
- [ ] **Step 3: Verificación en navegador** (puerto nuevo): `detectarConIA` contra `Testing/` (la del metal debe dar esquinas o al menos null limpio), flujo import → editor precargado, panel de cola con ítem simulado, chip leyendo (simulado), reconexión (aviso al fallar GIS en localhost).
- [ ] **Step 4: README** — actualizar el bloque "> Estado:" a Fase 2E (2-3 líneas) mencionando la descarga única de ~18 MB del motor IA.
- [ ] **Step 5: Commit; merge --no-ff a main; `git push origin main`; `git branch -f gh-pages main; git push origin gh-pages`.**
- [ ] **Step 6: Actualizar memoria del proyecto** (fotogastos-proyecto.md, sección Fase 2E).

---

## Self-review (hecho al escribir)

- **Cobertura del spec:** S1→T1/T2/T3/T4; S2→T5/T6; S3→T7; S4→T8; S5→T2; mantenimiento→T9. Sin huecos.
- **Consistencia:** `esquinasDeMascara(maskCanvas, wOrig, hOrig)` igual en T2 y T3; `detectarConIA` igual en T3 y T4; `rellenarPanel`/`alTerminarLectura`/`refrescarGastosSiVisible` definidas en T5 y usadas en T6/T8; `actualizarEntradaConReArchivo` devuelve `entrada` (T5) y T6 lo consume; `conectar({silencioso})` (T7) coincide con el spec.
- **Nota:** `aCuatroEsquinas` NO se exporta (T2 la usa dentro de detect.js; `esquinasDeMascara` vive en el mismo módulo, sin export extra).
